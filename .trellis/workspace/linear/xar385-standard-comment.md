Added `XartPro Linear Operating Standard v1` as the explicit project granularity guardrail.

This fills the gap that caused OpenAlice frontend rebuild to be initially created as a small task bundle instead of a complex project track.

New default:

- classify complexity before creating issues
- Class C work requires initiative + project + milestones + parent issue + dependency graph
- external agents must receive a Linear issue, branch, safe/forbidden files, and handoff format
